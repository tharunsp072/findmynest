import { Component, h, Prop, State, Event, EventEmitter, Method } from '@stencil/core';

@Component({
  tag: 'tenant-favorites',
  styleUrl: 'tenant-favorites.css',
  shadow: true
})
export class TenantFavorites {
    
  
  render() {
    return (
      <div class="layout">
        <h1>Favorites</h1>


      </div>
    );
  }
}